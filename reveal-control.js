window.RevealControl  = window.RevealControl || {
  id: 'reveal-control',
  init: ( deck ) => {
    initRevealControl(deck)
  }
}

const initRevealControl = function(Reveal){
  const config = Reveal.getConfig().revealControl || {}
}

